# Progress Report LaTeX Upload

Upload these files to Overleaf (all figures referenced by `main.tex`):

- `main.tex`
- `idf_dendrogram.png`
- `cardsort_cooccurrence_heatmap.png`
- `cardsort_agreement_bars.png`

Recommended Overleaf compiler: XeLaTeX.

If the course template already has its own `\documentclass`, preamble, title, or author block, copy only the content between `\begin{document}` and `\end{document}` from `main.tex` into the template body, then keep the figure file in the same Overleaf folder.
